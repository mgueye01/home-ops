---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # ./envoy.yaml — added in second commit after CRDs are installed by the HelmRelease
  - ./helmrelease.yaml
  - ./ocirepository.yaml
