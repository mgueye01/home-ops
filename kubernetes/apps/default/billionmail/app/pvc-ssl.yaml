---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: billionmail-ssl
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 100Mi
  storageClassName: ceph-filesystem
